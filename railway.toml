[build]
builder = "NIXPACKS"
buildCommand = "cd vibe-az-cms && npm install && npm run build"

[deploy]
startCommand = "cd vibe-az-cms && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
